// tailwind.config.js

module.exports = {
  content: ['./src/**/*.html', './src/**/*.js'], // Specify content paths
  theme: {
    extend: {
      // Optionally extend Tailwind CSS theme here
    },
  },
  plugins: [
    // Optionally configure additional Tailwind CSS plugins here
  ],
};
